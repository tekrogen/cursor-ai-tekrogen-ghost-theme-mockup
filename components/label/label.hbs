{{!-- Tekrogen · Label partial → mono pill / chip / tag. Pass variant + pillar. --}}
<span class="tk-label"{{#if variant}} data-variant="{{variant}}"{{/if}}{{#if pillar}} data-pillar="{{pillar}}"{{/if}}>{{text}}</span>
