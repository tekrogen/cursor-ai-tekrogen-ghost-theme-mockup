{{!-- Tekrogen · Manifesto card partial → a pillar card. --}}
<article class="tk-manifesto-card" data-pillar="{{pillar}}">
  <span data-tk-slot="label">.{{pillar}}</span>
  <h4>{{title}}</h4>
  <p>{{description}}</p>
</article>
