{{!-- Tekrogen · Badge partial → Ghost tag / status label. Pass pillar=org|studio|com|net
      for the tinted pillar pill + status dot (e.g. keyed off a post's primary_tag). --}}
<span class="tk-badge"{{#if pillar}} data-pillar="{{pillar}}"{{else}} data-variant="cyan"{{/if}}>{{name}}</span>
